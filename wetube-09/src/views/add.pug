extends templates/layout
include mixins/movie

block content
  h1=pageTitle

  form(method="POST")
    input(placeholder="Title", type="text" name="title")
    input(placeholder="Synopsis", type="text" name="synopsis")
    input(placeholder="Genres", type="text" name="genres")
    button Add